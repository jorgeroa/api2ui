{
  "name": "api2aux-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @api2aux/app dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "test:run": "pnpm -r test:run"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
